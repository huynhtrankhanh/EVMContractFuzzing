# EVMContractFuzzing
For prethesis and potentially thesis at HCMIU

## Strategy
Use EthereumJS running on QuickJS. Use QuickJS to compile EthereumJS code to C, then use AFL to fuzz the resulting C code.
